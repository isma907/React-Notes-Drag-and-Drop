import StickyNote from '../StickyNote/StickyNote'
import './Board.css'
const Board = () => {
    return (
        <section className="board">
            <StickyNote />
        </section>
    )
}

export default Board
